import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Tax credits (TDS)' };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
